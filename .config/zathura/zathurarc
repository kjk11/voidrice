set sandbox none
set statusbar-h-padding 0
set statusbar-v-padding 0
set page-padding 1
set selection-clipboard clipboard
map u scroll half-up
map d scroll half-down
map D toggle_page_mode
map r reload
map R rotate
map K zoom in
map J zoom out
map i recolor
map p print
map g goto top
# This doesn't work
map <C-c> feedkeys ":exec sh -c \"echo $FILE | sed 's/\.pdf//' | xclip -i -sel clip\"<Return>"
map <C-b> feedkeys ":exec sh -c \"echo $FILE | sed -e 's/\.pdf/.bib/' -e "s*$HOME/Documents/pdf/**" -e "s*^*$HOME/Documents/bib/*" | xargs -I {} nohup st nvim {}\"<Return>"
#map <C-b> focus_inputbar ":exec echo $FILE | sed -e 's/\.pdf//' -e "s|^|$HOME/Documents/bib/|" | x-clip -selection clipboard"
